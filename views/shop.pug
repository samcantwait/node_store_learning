extends layouts/main-layout.pug 

block content
    main
        h1 My Products 
        if prod.length > 0
            ul
                each product in prod
                    li #{product.title}
        else 
            p No products
            